
export class GameLogic {
    server: any;
    constructor(server) {
        this.server = server;
    }
    playerConnected = async (player) => {
    };
    playerDisconnected = async (player) => {
    };
}